import { Bayon, Space_Grotesk } from "next/font/google";

export const bayon = Bayon({
  subsets: ["latin"],
  weight: "400",
});

export const space_grotesk = Space_Grotesk({
  subsets: ["latin"],
  weight: "400",
});
